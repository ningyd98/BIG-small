# 有效性威胁

## 内部有效性

seed、仿真 determinism、worker 调度、cache、timeout 和环境版本都会影响结果。

## 外部有效性

任务集中在小型机械臂场景，仿真不等于真机，场景数量和机器人类型有限。

## 构念有效性

成功率不能覆盖全部安全性，通信次数不等于真实通信成本，仿真路径误差不等于真实控制误差。

## 结论有效性

样本量、多重检验、非独立样本、环境阻塞和模型随机性都限制结论。

没有真实硬件实验时，论文不能声明完成 sim-to-real 实证。
